/**
 * Placeholder for the generated TypeScript client.
 *
 * The client, its NDJSON stream parser and the release pipeline are Task A10
 * and are not implemented yet. Until then, consumers should generate against
 * the committed `openapi/openapi.json` or call the API directly; the schemas
 * re-exported here are the authoritative request/response types.
 */
export {
  BROKER_API_VERSION,
  CapabilitiesResponse,
  CreateSandboxRequest,
  ErrorResponse,
  ExecEvent,
  ExecRequest,
  NetworkMode,
  ReadyResponse,
  Sandbox,
  SandboxLimits,
  SandboxList,
  SandboxState,
  WORKSPACE_ROOT,
} from "@sandbox-broker/contracts";
